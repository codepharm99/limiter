VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (Supabase Edge Function secrets — never VITE_*):
# supabase secrets set TELEGRAM_BOT_TOKEN=...
# supabase secrets set INVOICE_PAYLOAD_SECRET=...      (openssl rand -hex 32)
# supabase secrets set TELEGRAM_WEBHOOK_SECRET=...     (openssl rand -hex 32)
# supabase secrets set PRO_STARS_PRICE=100             (optional, default 100)
TELEGRAM_BOT_TOKEN=
INVOICE_PAYLOAD_SECRET=
TELEGRAM_WEBHOOK_SECRET=
PRO_STARS_PRICE=
